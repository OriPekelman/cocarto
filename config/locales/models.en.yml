---
en:
  activerecord:
    attributes:
      field:
        field_type: Type
        label: Name
      layer:
        geometry_type: Geometry type
        linestring:
          one: line string
          other: line strings
        name: Name
        point:
          one: point
          other: points
        polygon:
          one: polygon
          other: polygons
    models:
      layer:
        one: Layer
        other: Layers
  helpers:
    submit:
      field:
        create: Add a field
      layer:
        create: Create a layer
