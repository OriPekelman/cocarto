<%# params: @row, @layer, @anonymous, @values %>

<section class="section">
  <div class="container">
    <h1>Modifier un formulaire</h1>
    <p>Cette restera accessible et partageable par <%= link_to "ce lien", edit_row_url(@row) %>
      <%= render "form", row: @row, layer: @layer, anonymous: @anonymous, values: @values %>
  </div>
</section>
