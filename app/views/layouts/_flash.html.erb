<div class="flash__container">
  <% flash.each do |flash_type, message| %>
    <div class="<%= class_names("flash__message", "flash__message--#{flash_type}") %>">
      <%= message %>
    </div>
  <% end %>
</div>
