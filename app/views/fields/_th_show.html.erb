<th class="field-<%= field.id %>">
    <div class="dropdown" data-controller="dropdown">
        <button aria-haspopup="true" aria-controls="dropdown-menu" data-action="click->dropdown#toggle">
        <span><%= field.label %></span>
    </div>
</th>
