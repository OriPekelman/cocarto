<h1>Editing Layer</h1>

<%= render 'form', layer: @layer %>

<h3>Fields</h2>
<ul>
<% @layer.fields.each do |field| %>
    <li> <%= render field %> </li>
<% end %>
</ul>

<h3>Add a new field:</h3>
<%= render 'fields/form' %>

<%= link_to 'Show', @layer %> |
<%= link_to 'Back', layers_path %>
