<%# params: layer %>
<%# renders a form for a layer name %>

<div id="layer-name" class="restricted">
  <h2>
    <%= form_with model: layer do |form| %>
      <%= form.text_field :name, onfocusout: "this.form.requestSubmit()" %>
    <% end %>
  </h2>
</div>
