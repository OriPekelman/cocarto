<p style="color: green"><%= notice %></p>

<h1>Roles</h1>

<div id="roles">
  <% @roles.each do |role| %>
    <%= render role %>
  <% end %>

  <%= render "form", role: Role.new(map: @map, user: User.new) %>
</div>
