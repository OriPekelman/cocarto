<tr id="<%= dom_id(@layer, :stats) %>" class="layer-table__tr__stats">
  <%= calculated_columns %>
  <% @layer.fields.each do |field| %>
    <td class="layer-table__td__numerical layer-table__td__stats calculated"> <%= number_to_human(field.sum) %> </td>
  <% end %>
  <td></td>
</tr>
